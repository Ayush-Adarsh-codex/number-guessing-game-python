# 🔢 Guess The Number Game

A simple, interactive command-line interface (CLI) game built with Python where players try to guess a randomly generated secret number within a limited number of attempts.

## 🚀 Features
- **Dynamic Difficulty:** Choose how many attempts you want before the game starts.
- **Interactive Feedback:** Get "higher" or "lower" hints after every wrong guess.
- **Input Validation:** Prevents the game from crashing if non-numeric values are entered.
- **Clean CLI Experience:** Minimalist design with clear status updates.

## 🛠️ Prerequisites
- Python 3.6 or higher installed on your machine. You can download it from [python.org](https://www.python.org).
